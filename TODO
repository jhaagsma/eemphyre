-change mysql.php to use the mysqli versions
-upgrade router for SPL Fixed Array in php5.3 (perhaps version check, or version comment out or something)
-minify the routing table by storing common attributes in parent (like auth type, base directory, skin)
-add set defaults for auth types, base directories, skin
